# ASSYLUM — план работ

Цель: из плоского прототипа сделать мрачную «объёмную» 2.5D-игру. Клетки не видны, свет фонарика
живой, как настоящий, всё работает быстро. Параллельно углубляем геймплей и держим код в порядке:
каждая фича живёт в одном месте, копий нет.

Порядок: **0 → 1 → 2 → 3 → 4** готовы. Дальше **5 → 6**: звук, злодеи. Потом **7** (пролог).
Этапы 8–10 — по ходу, по мере готовности ассетов.

---

## Правила кода (действуют на всех этапах)

1. **Один источник правды.** Константы, персонажи, предметы, сложность, пути к ассетам лежат в
   `src/data/*`, а не разбросаны по сценам.
2. **Дизайн-система в одном месте.** Цвета, шрифты, размеры и слои глубины описаны в `src/ui/theme.ts`.
   Кнопки, панели, слайдеры, карточки, всплывающие сообщения и полоски — в `src/ui/components.ts`.
   Сцены собирают интерфейс только из этих компонентов: никаких `add.rectangle` с цветом прямо в сцене.
3. **Фича = система.** У каждой механики (укрытия, терминалы, предметы, шум, свет, сеть) один модуль
   в `src/systems/`. Изменение состояния делается только в своей системе и объявляется
   типизированным событием (`keyCollected`, `runnerCaught`…); HUD, эффекты и сеть подписаны на
   события и ничего не меняют сами. Читать состояние других систем можно через общий `World`.
4. **Одна реализация алгоритма.** Поиск пути, прямая видимость и видимость клеток пишутся один раз
   (`src/world/grid.ts`) и используются везде.
5. **Типы вместо `any`.** Все персонажи — один класс `Actor`, поведение подключается снаружи
   (ввод, мозг из `src/ai/`, сеть). TypeScript в режиме `strict`.
6. **Проверки.** Юнит-тесты (vitest) для генератора, сетки и правил раунда; перед каждым пушем
   `npm run check` (типы, тесты, сборка) и прогон сценариев в браузере, включая мультиплеер.

---

## Этап 0 — исправление багов ✅ (сделано)

- **Режим за Foxmind:**
  - лиса не подбирает ключи и не выходит через выход;
  - счётчик «6/5» исправлен;
  - раунд заканчивается, когда все беглецы пойманы или сбежали;
  - у лисы свой экран итога: «ОХОТА УДАЛАСЬ» или «ДОБЫЧА УШЛА».
- **Боты:**
  - идут к ключу, до которого реально можно дойти;
  - сами взламывают терминалы (6 с), поэтому в режиме за лису беглецы теперь могут победить.
- **Генератор:** комната запирается, только если она действительно запечатана, а всё важное
  остаётся достижимым. Ушли три проблемы: босс внутри запертой комнаты, «дырявые» двери и
  непроходимые уровни. Проверено на 2100 уровнях.
- **HUD и туман:**
  - надписи не налезают друг на друга;
  - карта не просвечивает под панелью, на первом кадре и при тряске камеры.
- **Мультиплеер:**
  - пойманный или сбежавший игрок, включая хоста, переходит в режим наблюдателя, а не рвёт сессию;
  - конец раунда объявляет хост;
  - Foxmind-игрок ловит беглецов, и лишней ИИ-лисы больше нет;
  - синхронизируются укрытия, фонарик, побег и отключения;
  - если хост вышел, клиенты возвращаются в меню;
  - добавлена настройка `VITE_PEER_SERVER` для своего сервера PeerJS.
- **Код:** ключи, двери, поимка и конец раунда теперь обрабатываются в одном месте каждый;
  один экран результата вместо двух копий; общий выбор сложности.

---

## Этап 1 — фундамент (архитектура) ✅ (сделано)

Геймплей не менялся, поменялось устройство кода. `game.ts` на 1600 строк и остальные «всё в одном»
файлы разнесены по модулям; самый большой файл теперь — генератор уровня (~390 строк). Карта
модулей — в [README](../README.md#устройство-кода).

- **Одна реализация** поиска пути, BFS-расстояний, прямой видимости и поля зрения
  (`world/grid.ts`) — для генератора, ИИ, света и тумана. Копия в воркере удалена: на типизированных
  массивах расчёт занимает доли миллисекунды, отдельный поток не нужен.
- **Дизайн-система:** `ui/theme.ts` + `ui/components.ts`, все экраны собраны из них. Шрифты с
  кириллицей (JetBrains Mono, Rubik Wet Paint) идут вместе с игрой, интернет не нужен.
- **Сущности:** один класс `Actor` для всех персонажей; поведение — снаружи (ввод, мозг ИИ, сеть).
- **Системы и события:** ключи, двери, укрытия, свет, шум, раунд… каждая в своём файле; изменения
  объявляются событиями, на них подписаны HUD, эффекты и сеть.
- **Уровень по seed:** в мультиплеере передаётся одно число вместо всей карты.
- **Одна модель света:** «видит ли лиса игрока» считается по тому же свету ламп, что рисуется на
  экране, и стены его теперь загораживают.
- **HUD — отдельная сцена** поверх игры: эффекты мира (свет, тряска, зум) его не задевают.
- **Производительность:** пол и стены запекаются в 6 больших текстур вместо ~6000 спрайтов; стены —
  невидимый тайлмап для коллизий вместо ~2000 физических тел.
- **Сеть:** сообщения типизированы (`net/protocol.ts`), позиции хост шлёт одним пакетом на всех;
  пинг раз в секунду — хост замечает закрытую вкладку за ~6 с (раньше не замечал вовсе, и раунд
  зависал).
- **Настройки** (музыка, счётчик FPS) и последняя сложность сохраняются в браузере.
- **TypeScript `strict`**, ни одного `any`. Тесты: `npm test` (генератор — 900 уровней, сетка,
  правила раунда), `npm run check` — типы + тесты + сборка.

---

## Этап 2 — графика 2.5D и свет ✅ (сделано, кроме ракурсов персонажей)

Подключены все 90 картинок набора (поверхности, декали, мебель, двери, выход, иконки HUD).
Для всего, чего ещё нет, работают процедурные заглушки с теми же ключами, и новые файлы встают
на их место без правки кода.

- **Камера и чёткость:** приближение ×1,5 (клетка ≈ 48 px); холст рендерится в разрешении экрана
  (до 2×), текст растеризуется в том же масштабе. Логика игры не менялась.
- **Пол без сетки:** большие бесшовные текстуры по типам комнат, привязанные к миру, а не к клетке;
  декали (кровь, следы волочения, грязь, трещины, лужи, осколки, бумаги) раскиданы по seed;
  мягкое затемнение у стен (AO). Всё запекается при старте в несколько больших текстур.
- **Стены 3/4:** видна передняя грань; верх стены поднят и закрывает тех, кто стоит за стеной.
  Запертые двери — металлические двери в проёме; выход — двойные двери в северной стене комнаты.
- **Мебель:** 29 видов по типам комнат, вдоль стен, с коллизиями. Каждая расстановка проверяется:
  ключи, терминалы и выход остаются достижимыми, отрезанных углов нет. Кровати и шкафчики —
  укрытия, шкафчики стоят у стен. Лампы висят на стенах.
- **Свет (шейдер, 2 прохода):**
  - тени от стен считаются лучом по сетке для каждого пикселя, края мягкие;
  - прямая видимость от глаз игрока — вместо квадратного тумана;
  - источники: лампы (часть — красные аварийные, часть мигает; после пробуждения Желочи всё
    краснеет), фонарики с мягким конусом и дымкой в воздухе, вспышка лисы, свечение терминалов,
    выхода и ключей;
  - тьма по канону (этап 3): видно только освещённое и то, что в прямой видимости; памяти карты
    нет — сзади и за стеной полная темнота, верх стен освещается попиксельно, без квадратов.
- **Объём без нормал-мап:** рельеф считается из яркости текстуры прямо в шейдере и освещается со
  стороны игрока — плитка, швы и трещины «лепятся» в луче фонарика.
- **Пост-эффекты:** холодная бесцветная темнота, зерно, виньетка, свечение; красный пульс и
  хроматическая аберрация, когда охотник рядом.
- **Персонажи:** тень под ногами, покачивание и наклон при ходьбе, разворот, сортировка по глубине
  с мебелью и стенами; пыль, видимая только в свете. Имена и метки не темнеют — они в HUD.
- **Одна модель света для картинки и ИИ:** лиса «видит» игрока по тем же лампам, что на экране.
- **Пресеты качества** (низкое / среднее / высокое) в настройках.
- **Конвейер ассетов:** исходники лежат в `art/`, `npm run assets` делает WebP нужного размера в
  `public/assets/` и манифест. 165 МБ исходников → 3 МБ в игре.

**Осталось по этапу 2:**
- ракурсы персонажей (вперёд / назад / вбок) — нужен арт, см. ASSETS.md, раздел F;
- кровавые следы за персонажем, если он прошёл по крови;
- панели и кнопки из `ui/panel`, `ui/button` в меню (это этап 5);
- проверить FPS на реальной встроенной видеокарте (в тестовой среде рендер программный).

---

## Этап 3 — геймплей ✅ (сделано)

Раунд стал дольше и страшнее: темно, все ходят шагом, бег слышно, лиса умная, а чтобы выйти,
нужно не только собрать ключи, но и вернуть питание.

- **Движение:** все на 20% медленнее; по умолчанию шаг, **Shift** — бег, **C** — тихий шаг.
  Выносливость: бег её тратит, без сил — медленный шаг и тяжёлое дыхание, которое слышно.
  Фонарик светит туда, куда смотрит мышь (или куда идёшь, если мышь не трогать).
- **Тьма (канон):** персонажа видно, только если он в прямой видимости **и** освещён — лампой,
  лучом, химсветом — или стоит вплотную. Кто идёт сзади в темноте — не виден; выдать его может
  фонарик или шаги. Лампы стали пятнами света, мигают, красные аварийные остаются.
- **Шум и слух:** у каждого звука свой радиус, стены его глушат. Бег слышно далеко, шаг — рядом,
  тихий шаг — никак. Двери, бутылки, тревога терминала, дыхание в шкафчике, свист. На экране —
  круги от того, что ты слышишь: синие — люди, красные — монстры.
- **Лиса (ИИ):** поле зрения спереди, видит освещённых и тех, кто близко в темноте; время
  реакции по сложности. Слышит и идёт проверять, помнит, куда ты бежал, прочёсывает район,
  открывает шкафчик, если видела, как ты залез, или услышала дыхание; патрулирует места, где
  кого-то замечала; сторожит выход, когда побег близко; вспышкой проверяет тёмные углы.
- **Желочь:** идёт по запаху — по следу ближайшего беглеца с отставанием в несколько секунд;
  увидев — бросается (быстрее шага), потом выдыхается; может разорвать шкафчик, где кончается след.
  Рядом с ней лампы захлёбываются. За 30 с до пробуждения — предупреждения, свет начинает дрожать.
- **Боты:** делят цели (у каждого своя), ходят тихо, обходят места, где видели или слышали монстра,
  бегут и прячутся, когда их гонят, светят только в спокойствии, меняют батарейки.
- **Предметы** (3 слота, клавиши 1/2/3): батарейка, адреналин, бутылка (бросок — шум, лиса идёт
  на звук), седативное (срабатывает само, если схватили), химсвет (бросок — свет на 90 с), записки
  с лором, обрывок карты (показывает ближайшие цели).
- **Питание:** предохранители (2–4 по сложности) нужно отнести в щиток на стене; пока их нет,
  выход не откроется, даже с ключами.
- **Двери** в проёмах: E — открыть/закрыть, закрытая режет свет, обзор и звук; лиса открывает
  с задержкой, Желочь выбивает. Терминалы висят на стене у двери и гаснут после открытия.
- **Укрытия:** шкафчики и кровати (и кушетки в изоляторах). **Пробел** — задержать дыхание, когда
  монстр рядом; не хватит воздуха — выдох услышат. Охотник-игрок обыскивает укрытия (E).
- **Терминал:** цифры кода мелькают по одной — запомни и набери; ошибка поднимает тревогу.
- **Захват:** седативное или способность Wite — вырваться, лиса оглушена на 2,5 с.
- **Уровни:** запутанная геометрия осталась; добавлены колонны в больших залах, проломы между
  комнатами с обломками, двери с косяками; слившиеся комнаты получают один тип и один пол; мебель
  стоит по смыслу (столы в центре, шкафы у стен, кровати рядами изголовьем к стене).
- **Тонкие стены:** соседние комнаты стоят через одну общую стену, и стена в одну клетку между
  помещениями — тонкая перегородка, а не «куб»: пол виден по обе стороны, к ней можно подойти
  вплотную, свет и взгляд режет только сама перегородка. Толстыми остались внешние стены
  и несущие массивы. Двери в тонких стенах такой же высоты, как стена; тяжёлые железные двери
  и выход — усиленные блоки. Верх стен теперь сортируется с персонажами по рядам: кто стоит
  перед стеной, рисуется поверх неё (голову лисы и Желочи больше не срезает).
- **Мультиплеер:** всё новое синхронизируется — предметы, броски, двери, предохранители, шумы,
  обыск укрытий, «вырвался из лап».

| Персонаж | Способность (сделано) |
|---|---|
| Naumi | +10% скорость, бег тише |
| Kuruna | в темноте охотник замечает его вдвое ближе |
| Wite | чуть медленнее; раз за раунд вырывается из лап; видит предметы в темноте |
| Sumrak | видит ключи и укрытия сквозь тьму; код терминала короче |
| Yoko | больше выносливости; свист (Q) уводит лису за собой |

**Осталось по этапу 3:**
- мебель боком у боковых стен, декор стен и новые комнаты (кабинет, гидротерапия) — как только
  будет арт (ASSETS.md, раздел H; код для шкафчиков, дверей, щитка и декора уже ждёт картинки);
- боты с бутылками и химсветом, несколько этажей;
- баланс по живым партиям.

---

## Этап 4 — правки после плейтеста ✅ (сделано)

- **Верх стен без клиньев.** Верх стены теперь освещён и виден так же, как пол вплотную к стене
  со стороны источника, а не по лучу изнутри самой стены. Клиньев в каждой клетке больше нет ни
  у тонких, ни у толстых стен. Перегородки чужих комнат остаются тёмными.
- **Размеры мебели.** У мелких вещей своя высота на экране (беглец — 30 px): унитаз 58 → 18,
  бочка 48 → 22, стул 47 → 22, капельница 71 → 34, шкафчик 51 → 40. Вещь у стены прижимается
  к ней. Коллизия идёт по видимому основанию, к бочке подходишь почти вплотную. Для ИИ и путей
  клетка по-прежнему занята.
- **Вещи из-за стены не видны.** Мебель, шкафчики, кровати, лампы, терминалы и щиток видны,
  только пока пол под ними в прямой видимости. Верхушка шкафчика из соседней комнаты больше не
  торчит над стеной.
- **Тени.**
  - Мебель и персонажи отбрасывают мягкую тень-силуэт от фонариков и ламп, до двух источников
    на предмет. Тень темнее у основания, к концу тает, у стены обрывается.
  - Под мебелью есть тень касания.
  - Качество: низкое — только тени касания, среднее — ещё от фонариков, высокое — от всех.
- **Обводка.** Предметы на полу, ключи и предохранители обведены тонким тёплым контуром, он
  пульсирует. То, на что сработает E, обведено ярко: дверь, шкафчик, кровать, терминал, щиток.
  В темноте обводки не видно, как и самих предметов.
- **Дверь из проёма.**
  - E в проёме: персонаж отступает на свою сторону (изнутри остаётся внутри, посередине —
    туда, куда смотрит), и дверь захлопывается.
  - Створка поворачивается на петле, на бегу хлопок громче.
  - Если в проёме кто-то другой, закрыть нельзя.
- **Шаги.** Вместо ровных кругов — мягкие рваные волны, по две на шаг. У монстров волны
  тяжелее и дрожат, из-за стены глуше и с разрывами. В свете видны следы ног, после крови они
  кровавые. На бегу из-под ног летит пыль.

---

## Этап 5 — звук (твои файлы)

**Получено 29 файлов.** Разобрал каждый: длина, громкость, тишина по краям, сколько повторов
внутри. Длинные файлы оказались сериями (16 шагов, три скрипа двери, пять смешков), их нарежу
на варианты.

### 5.1. Обработка
- **Исходники** переезжают из `public/sound/` в `art/sound/`, как картинки лежат в `art/`.
  Сейчас все 13 МБ целиком уходят в сборку игры.
- **`npm run assets`:**
  - нарезает звуки и убирает тишину по краям;
  - делает мягкие края, без щелчков;
  - выравнивает громкость, переводит в моно и сжимает;
  - фон и гул склеивает в петлю без шва.
- **Результат** ложится в `public/assets/sound/`: около 1 МБ вместо 13.
- **Одна таблица.** Нарезка, громкость и назначение каждого звука записаны в
  `src/data/sounds.ts`. Новый звук — одна строчка.
- **Старая музыка** «Asylum Echoes» (8,7 МБ) уходит совсем. В игре останется только атмосфера,
  в меню — твой новый трек.

### 5.2. Движок звука (`core/audio.ts`, одно место)
- **Каналы.** Музыка, эффекты и окружение — у каждого свой ползунок в настройках.
- **Позиция.** Громкость и панорама зависят от расстояния.
- **Стены.** За стеной звук глуше, срезаются высокие частоты. За закрытой дверью — ещё глуше.
- **Разнообразие.** Каждый шаг — случайный вариант нарезки с лёгким разбросом высоты, чтобы
  не было «пулемёта».
- **Одна система с кругами шума.** Ты слышишь то же, что видишь кругами, и то же, что слышит ИИ.
  Игрок сзади в темноте выдаёт себя шагами, как и задумано.

### 5.3. Что куда

| Файл | Нарезка | Где звучит |
|---|---|---|
| шаг ботинком (16 шагов) | 8 лучших | ходьба |
| шаг бегом (2 файла, ~30 шагов) | 10 | бег |
| скрип двери (2 файла) | 4–5 скрипов | открыть дверь; короткий скрип — закрыть |
| ключ в замке и засов | поворот, щелчок | железная дверь отперта |
| шкафчик: открыть (2), закрыть (1) | 3–4 и 1 | залезть, вылезти, лиса обыскивает |
| терминал: бип (3), ошибка, успех | по одному | цифра кода, ошибка, дверь открыта |
| щелчок фонарика (4 щелчка) | вкл и выкл | фонарик |
| звон ключей | 1 | подобрал ключ |
| сердцебиение (3 файла) | отдельные удары | чем ближе монстр, тем чаще; ритм задаёт игра |
| смех лисы (2 файла) | 4–5 коротких | заметила, поймала, изредка издалека |
| рык лисы (2 файла) | 3–4 | погоня, захват |
| рёв | 1 | Желочь |
| крик | 1 | кого-то поймали |
| скример (2) | 2 | монстр внезапно рядом, захват |
| гул лампы (2) | петля | у ближайших ламп, треск в такт миганию |
| тёмный фон | петля | фон всей игры вместо музыки |
| main menu | петля | главное меню |

### 5.4. Чего не хватает
- **Первая очередь (P1)** — для механик этапа 3 и злодеев:
  - двери: захлопнутая и выбитая;
  - бутылка: разбилась;
  - дыхание: тяжёлое и вдох после задержки;
  - кровать: скрип;
  - питание: предохранитель и включение;
  - выход открывается;
  - шаги Желочи и когти лисы;
  - подбор предмета.
- **Вторая очередь (P2):**
  - химсвет, свист, вспышка лисы;
  - захват и «вырвался», укол, батарейка;
  - записка, умирающая лампа;
  - дальние звуки здания;
  - кнопки меню;
  - короткие темы победы и поражения.
- **Где взять:** список с промптами — в ASSETS.md, раздел «Звук».
- **Пока их нет,** эти места молчат или берут похожий звук из того, что есть.

---

## Этап 6 — злодеи: злодеем может стать любой

**Решено:**
- **Злодей — заражённый персонаж.** Лиса и Желочь как отдельные персонажи уходят. У каждого из
  пяти героев два скина: здоровый и заражённый. Арт будет завтра (ASSETS.md, раздел F2), а до
  него игра рисует заглушку — тот же спрайт в болезненном цвете.
- **Роль отдельно от персонажа.** Выбираешь персонажа, роль даёт матч.
- **Пока два общих набора навыков**, злодей берёт один. Потом придумаем каждому свои.
  - **«Лиса»:** видит в темноте, вспышка, обыск укрытий, быстрая.
  - **«Желочь»:**
    - **запах:** видит след ближайшего беглеца с отставанием на несколько секунд, а в темноте
      видит хуже лисы;
    - **рывок (Shift):** короткий бросок быстрее бега, потом одышка;
    - **удар (E):** выбивает дверь с одного удара, вытаскивает беглеца из шкафчика или из-под
      кровати;
    - **рёв (R, перезарядка):** у беглецов рядом на пару секунд гаснут фонарики, а рёв слышат все;
    - **лампы рядом захлёбываются:** злодей ходит в темноте, но мигание его выдаёт;
    - **слабости:** медленный шаг и тяжёлые шаги, которые слышно издалека.
- **Босса больше нет:** никто не просыпается по таймеру.
- **Нарастание вместо босса.** Через несколько минут здание «просыпается»: лампы гаснут одна за
  другой, злодей ускоряется. Конец раунда не проседает.
- **Одиночная игра.** Выбираешь персонажа и роль. Беглецом играешь против ИИ-злодея: это
  заражённый из других героев, набор выбираешь ты или случай. Злодеем играешь против ботов.
- **Мультиплеер.** В лобби злодеем становится доброволец, а если никто не вызвался — случайный
  игрок. Злодей один, при 5+ игроках можно двух. Позже можно тайно, чтобы все узнали в прологе
  (этап 7).

---

## Этап 7 — пролог (идея, продумаем потом)

Записал, чтобы не потерять:
- **Встреча.** Все игроки просыпаются в одной-двух комнатах и встречаются.
- **Головоломки.** Их решают вместе: например, код разделён между игроками, или без
  предохранителя не открыть палату.
- **Превращение.** Игроки выходят в коридор к «выходу», но дверь захлопывается, гаснет свет,
  и один из них начинает издавать страшные звуки: он превращается.
- **Основная карта.** Экран гаснет, и все просыпаются на основной карте в разных местах:
  кто-то встаёт с кровати, кто-то вылезает из шкафчика.
- **Одиночная игра** — с ботами. Для тех, кто уже видел пролог, есть кнопка «пропустить».

**Что понадобится:**
- карта пролога, нарисованная вручную (не генератором);
- простая катсцена: затемнение, звук, камера;
- 2–3 головоломки;
- синхронизация в сети.

Пролог связан с этапом 6: он решает, кто станет злодеем.

---

## Этап 8 — интерфейс
- **Меню:**
  - настройки (звук, качество графики, управление) — кнопка «Настройки» сейчас пустая;
  - пауза.
- **HUD:** новый, с иконками, полосками выносливости и батарейки.
- **Прочее:** подсказки для первого запуска, структура под локализацию ru/en.

## Этап 9 — мультиплеер
- **Лобби:**
  - уровень по seed;
  - реванш без выхода из лобби;
  - переподключение.
- **Сервер:** свой PeerJS (готово через `VITE_PEER_SERVER`) и TURN для сложных сетей.

(Синхронизация предметов, дверей, шума и фонариков сделана на этапе 3.)

## Этап 10 — оптимизация и выпуск
- **Сжатие ассетов:**
  - звук — на этапе 5 (13 МБ → около 1 МБ);
  - арт меню 2,7 МБ → WebP ~0,3 МБ;
  - атласы текстур.
- **Бандл 1,6 МБ:** урезанная сборка Phaser и ленивая загрузка.
- **Публикация:** GitHub Pages или itch.io.

---

## Что нужно от тебя
1. Заражённые скины — ASSETS.md, раздел F2.
2. Звуки из ASSETS.md (раздел «Звук»), сначала P1.
3. Ассеты помещений — ASSETS.md, раздел H (двери, щиток, шкафчики сбоку, декор, новые комнаты).
4. Спрайты персонажей в 3 ракурсах (раздел F) — когда будут хорошие референсы.
5. Плейтесты: баланс, скорость, длина раунда.
